export enum GameStatus {
  SUCCESS = "SUCCESS",
  FAIL = "FAIL",
  PRE_START = "PRE_START",
  START = "START",
}

export enum GameRoutes {
  MENU = "/",
  GAME = "/game",
  RESULT = "/result",
}

export enum GameIcons {
  PLAY = "play",
  CONFIG = "config",
  DONE = "done",
  BACK = "back",
}

export enum GameSounds {
  CLICK = "click",
  NOT_MATCHED = "not_match",
  SUCCESS = "success",
  FAIL = "fail",
  STOP = "stop",
}
